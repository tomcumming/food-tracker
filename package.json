{
  "name": "food-tracker",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "format": "prettier --write src dist/*.html scripts package.json tsconfig.json",
    "check": "tsc",
    "precommit": "npm run format && npm run check"
  },
  "dependencies": {
    "preact": "^10"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "prettier": "3",
    "tailwindcss": "^4",
    "typescript": "6"
  }
}
